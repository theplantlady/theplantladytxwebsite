/*
import { getDocs } from '@firebase/firestore'
import {claimsCol} from "../useDb";

export const logClaimData = async () => {
    const claimDocs = await getDocs(claimsCol)
    claimDocs.docs.forEach((claimDoc) => {
        const claim = claimDoc.data()
        console.log(claim)
    })
}*/
